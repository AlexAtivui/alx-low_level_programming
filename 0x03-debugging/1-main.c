#include <stdio.h>

/**
 * main - causes of infinite loop
 * Return: 0
 */
int main(void)
{
	int i;

	printf("infinte loop incoming :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("Infinite loop avaoidable! \\o/\n");
	
	return (0);
}
